'use strict'

const request = require('request')
const mapper = require('../mapper')

module.exports = {
	postCalendarAllDayEvent
}

function postCalendarAllDayEvent(access_token, milestone, cb) {
	//TODO: post stuff
}